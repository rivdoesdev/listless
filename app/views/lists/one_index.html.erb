<div class='group-index'>
  <h4>Feeling like you can't even get out of bed? </h4>
  <p>These are the lists you marked as the lowest level of difficulty. Try starting with one of these and see how you feel. </p>

  <div class='grouped-lists'>
    <% if lists.where(archived: false).count.zero? %>
      <p>You don't have any lists ranked 1/5 or lower :(</p>
    <% else %>

      <% unless lists.where(difficulty: 1).empty? %>
        <ul class='collection'>
          <p class='list-collection'>1
          <% lists.each do |list| %>
            <% unless list.archived? %>
              <% if list.user == current_user %>
                <% if list.difficulty == 1 %>
                <li class='list-collection'> <%= link_to list.title, list %> </li>
                <% end %>
              <% end %>
            <% end %>
          <% end %></p>
        </ul>
        <% end %>
      <% end %>

      <div class='grouped-btn'>
      <%= link_to "All Lists", lists_path, class: "btn" %>
      </div>
    </div>
  </div>
