<h4>Congratulations, you're feeling like you can scrape by! </h4>
<p>These lists are marked with 3/5 or less on the difficulty scale. Any of these lists should match up with your energy today. </p>

<% if lists.where(archived: false).count.zero? %>
  <p>You don't have any lists ranked 3/5 or lower :(</p>
<% end %>

<p>3
<% lists.each do |list| %>
  <% unless list.archived? %>
    <% if list.user == current_user %>
      <% if list.difficulty == 3 %>
      <li> <%= link_to list.title, list %> </li>
      <% end %>
    <% end %>
  <% end %>
<% end %></p>

<p>2
<% lists.each do |list| %>
  <% unless list.archived? %>
    <% if list.user == current_user %>
      <% if list.difficulty == 2 %>
      <li> <%= link_to list.title, list %> </li>
      <% end %>
    <% end %>
  <% end %>
<% end %></p>

<p>1
<% lists.each do |list| %>
  <% unless list.archived? %>
    <% if list.user == current_user %>
      <% if list.difficulty == 1 %>
      <li> <%= link_to list.title, list %> </li>
      <% end %>
    <% end %>
  <% end %>
<% end %></p>

<%= link_to "All Lists", lists_path, class: "btn" %>
